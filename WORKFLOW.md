# 每週經課靈修生成工作流（Workflow Specification）

本專案旨在將每週教會經課與 NotebookLM 研經資料庫，自動化轉化為針對不同族群（「青年」與「父母與青少年」）的六日（週一至週六）靈修材料。

---

## 1. 核心原則與預設設定（Default Policy）

1. **檔案覆蓋策略：預設「重新生成並覆蓋」（Overwrite = true）**：
   * 當目標日期目錄或檔案已存在時，工作流預設**直接重新生成並覆蓋現有檔案**。
   * 確保每次執行都能取得最新、最完整的內容，無需手動刪除舊檔。

2. **執行模式：預設「模式 B：分步／逐日深耕生成」（Iterative Deep-Dive Mode）**：
   * 為確保每一天的信息具備充分的釋經厚度（1,200–1,500 字散文）、希臘原文拆解、筆記本專屬喻道故事改寫與深刻真摯的禱告詞，**工作流預設採用分步生成（先定主題進程，再分步/逐日產生每日信息）**，避免一次性大量輸出導致篇幅縮水或細節稀釋。

---

## 2. Spark 環境配置與前置準備（Setup in Spark）

在 Gemini Spark 中執行此工作流，請確認以下兩項設定：

1. **已連結本機資料夾（Connected Folders）**：
   * 請確認 Spark 的 Connected Folders 已加入本專案根目錄：`/Users/winston/Repo/QT-with-Tychicus`。
   * 這允許 Spark 直接讀取 `prompts/` 中的指令模板，並自動將產出檔案寫入 `data/`。

2. **掛載 NotebookLM 筆記本（NotebookLM Integration）**：
   * 在 Spark 輸入框旁點選附件「+」（或資料來源整合），選擇對應的 **NotebookLM 筆記本**（例如「研經推基古：哥林多後書」）。
   * 掛載後，Spark 即具備該經卷完整的高階釋經資料庫（含上下文歷史、希臘原文分析、教義神學等），生成時會優先汲取其中的解經亮光。

---

## 3. 是否需要建立新工作？（Task Management Strategy）

### **強烈建議：每一週（或每個新進程）建立一個「新工作」（New Chat / Task）**

雖然在同一個對話中可以繼續進行下一週，但採用**「一週一工作」**是最佳實踐，原因如下：
1. **防止上下文過度膨脹（Context Window Bloat）**：
   * 每一週工作流在模式 B 深耕下會產出長篇高質量內容（兩份主題進程 + 兩份六日完整靈修信息，總字數達 2 萬字以上）。若數週疊加在同一個對話中，龐大的歷史紀錄會消耗大量上下文，可能導致生成速度變慢或格式精確度下降。
2. **乾淨的解經資料庫對應**：
   * 若進度切換至不同書卷（例如從《哥林多後書》換到《加拉太書》或《以弗所書》），在新工作中直接掛載對應卷別的 NotebookLM 筆記本，能確保資料來源純淨，不被前卷書的文獻干擾。
3. **專案管理清晰**：
   * 每個工作專注於單一週次的產出與檢驗，若需要針對該週細節微調（如重新潤飾某天的禱告或信息），紀錄單純且易於追溯。

---

## 4. 目錄與檔案結構（Directory & File Structure）

```
QT-with-Tychicus/
├── prompts/
│   ├── topics_for_youth.txt      # 青年篇主題規劃 Prompt
│   ├── topics_for_family.txt     # 父母與青少年篇主題規劃 Prompt
│   ├── qt_for_youth.txt          # 青年篇每日信息 Prompt
│   └── qt_for_family.txt         # 父母與青少年篇每日信息 Prompt
├── data/
│   └── <YYYY-MM-DD>/
│       ├── topics_for_youth.md   # [產出] 青年主題與六日進程規劃
│       ├── topics_for_family.md  # [產出] 家庭主題與六日進程規劃
│       ├── qt_for_youth.md       # [產出] 青年週一至週六完整靈修（含頂部範圍、總主題、簡述）
│       └── qt_for_family.md      # [產出] 家庭週一至週六完整靈修（含頂部範圍、總主題、簡述）
└── WORKFLOW.md                   # 本工作流指南
```

---

## 5. 標準操作流程（Mode B SOP）

模式 B 採用「**先定骨架、再分步深耕**」的兩階段推進流程：

```
步驟 1：建立新工作 (New Task in Spark)
   │
步驟 2：確認 Connected Folders 包含 QT-with-Tychicus，並掛載對應 NotebookLM
   │
步驟 3【第 1 階段：主題骨架】：發送指令生成兩份「主題進程規劃」
   │   └─ Spark 建立 data/<YYYY-MM-DD>/ 並覆蓋寫入 topics_for_youth.md 與 topics_for_family.md
   │
步驟 4【第 2 階段：每日深耕】：發送指令逐日（或分組）深耕生成每日信息
       ├─ 指令範例 A（單日深耕）：「請根據主題規劃，生成週一的青年篇與家庭篇信息」
       └─ 指令範例 B（全週依序深耕）：「請根據已定主題規劃，依序深耕生成週一至週六完整信息並覆蓋寫入」
          └─ Spark 維持每日 1,200–1,500 字深度散文品質，依序寫入/覆蓋 qt_for_youth.md 與 qt_for_family.md
```

### 標準觸發指令範例

#### 階段一：生成每週主題規劃（骨架）
> **「請根據 WORKFLOW.md，為 2026-09-21 的經課『哥林多後書 1:12-22』生成本週主題進程規劃（青年篇與家庭篇）。」**

#### 階段二：深耕生成每日完整靈修材料
* **選項 1（單日逐步細修）**：
  > **「請根據主題進程規劃，為週一（1:12-14）生成青年篇與家庭篇完整靈修信息。」**
* **選項 2（全週依序深耕覆蓋）**：
  > **「請根據主題進程規劃，以模式 B 逐日深耕標準（每篇 1,200–1,500 字散文），生成週一至週六全部內容並覆蓋更新至 `qt_for_youth.md` 與 `qt_for_family.md`。」**

---

## 6. 工作流內部執行流水線（Pipeline Details）

### 階段一：建立目錄與初始化
* 依據輸入日期，確認或建立目錄：`data/<YYYY-MM-DD>/`。
* 若檔案已存在，以覆蓋寫入（`Overwrite: true`）為預設行為。

### 階段二：生成每週主題規劃（Topics Planning）
1. **青年篇主題規劃**：
   * 讀取 `prompts/topics_for_youth.txt`。
   * 結合經文與 NotebookLM 釋經資料，切入青年處境（自我認同、職場生涯、挫折壓力）。
   * 覆蓋輸出至 `data/<YYYY-MM-DD>/topics_for_youth.md`（含總主題、200–250 字說明、週一至週六經文範圍/主題/核心概念導引）。
2. **家庭篇主題規劃**：
   * 讀取 `prompts/topics_for_family.txt`。
   * 結合經文與 NotebookLM 釋經資料，切入家庭處境（教養焦慮、溝通張力、情緒風暴、力不能勝之軟弱）。
   * 覆蓋輸出至 `data/<YYYY-MM-DD>/topics_for_family.md`（含總主題、200–250 字說明、週一至週六經文範圍/主題/核心概念導引）。

### 階段三：模式 B 每日深耕靈修材料生成（Daily QT Generation）
1. **青年篇完整信息**：
   * 依據 `prompts/qt_for_youth.txt` 與產出的 `topics_for_youth.md`。
   * 最頂端標註：`【經文範圍】`、`【總主題】`、`【簡述】`。
   * 循序生成週一至週六內容，每日包含：
     * **【經文】**：新標點和合本完整字句。
     * **【主題】**：沿用進程規劃名稱。
     * **【信息】**：1,200–1,500 字流暢散文（嚴禁條列或清單），融入歷史背景、原文核心字義、神學詮釋，並優先引用 NotebookLM 來源中的喻道故事改寫。
     * **【建議禱告】**：200–250 字第一人稱真誠交託禱告。
     * **【延伸研讀】**：3 處跨卷經文（書卷 章:節｜4–5 句話說明內在關聯）。
     * **【默想問題】**：1 個高質感開放式反思題。
   * 覆蓋寫入 `data/<YYYY-MM-DD>/qt_for_youth.md`。
2. **家庭篇完整信息**：
   * 依據 `prompts/qt_for_family.txt` 與產出的 `topics_for_family.md`。
   * 最頂端標註：`【經文範圍】`、`【總主題】`、`【簡述】`。
   * 循序生成週一至週六內容，每日包含：
     * **【經文】**：新標點和合本完整字句。
     * **【主題】**：沿用進程規劃名稱。
     * **【信息】**：1,200–1,500 字流暢散文（嚴禁條列或清單），兼顧父母承擔重壓的撫慰與青少年內在掙扎的同理，優先引用 NotebookLM 來源中的喻道故事改寫。
     * **【建議禱告】**：200–250 字第一人稱家庭交託禱告。
     * **【延伸研讀】**：3 處跨卷經文（書卷 章:節｜4–5 句話說明內在關聯）。
     * **【默想問題】**：1 個適合個人或家庭餐桌敞開對話的開放式問題。
   * 覆蓋寫入 `data/<YYYY-MM-DD>/qt_for_family.md`。
